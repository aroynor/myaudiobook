# Copy this file to .env and adjust for your homeserver
# Run: cp .env.example .env

# Your Linux user/group ID (run `id -u` and `id -g` on the homeserver to find these)
PUID=1000
PGID=1000

# Timezone, e.g. Asia/Kolkata, Europe/Oslo, etc.
TZ=Europe/Oslo
